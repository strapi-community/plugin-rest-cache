# Benchmarks

> Generated by `.github/workflows/benchmarks.yml`. Do not edit by hand - re-run the workflow instead.

## Context

- Generated: `2026-08-14T02:09:00.521Z`
- Commit: `ee7515812dc0b64e615df3a98627126cfb674e31`
- Plugin version: `5.1.0`
- Strapi version: `5.52.0`
- Endpoint: `/api/homepage?populate=*`
- Load: 50 connections, pipelining 1, 30s per scenario (5s warmup)

| Provider | Runner | Cores | Node |
|---|---|---:|---|
| `memory` | GitHub Actions 1000003579 | ? | ? |
| `redis` | GitHub Actions 1000003580 | ? | ? |

**Read the ratios, not the absolute numbers.** Each provider runs on its own runner, so throughput is not comparable between the columns below, and not comparable between runs at all. What is meaningful is each scenario against the cache-disabled baseline measured on that same runner, in that same job.

## Speedup vs cache disabled

| Scenario | `memory` | `redis` |
|---|---:|---:|
| Cache enabled, ETag off | 3.1x | 3.1x |
| Cache enabled, ETag on | 3.0x | 2.9x |
| Cache enabled, conditional requests (304) | 3.2x | 3.1x |
| Cache enabled, every request a distinct key | 1.0x | 1.0x |

## Requests per second

| Scenario | `memory` | `redis` |
|---|---:|---:|
| Cache disabled (reference) | 334 | 320 |
| Cache enabled, ETag off | 1,038 | 988 |
| Cache enabled, ETag on | 1,016 | 913 |
| Cache enabled, conditional requests (304) | 1,072 | 994 |
| Cache enabled, every request a distinct key | 332 | 311 |

## Latency p50

| Scenario | `memory` | `redis` |
|---|---:|---:|
| Cache disabled (reference) | 146 ms | 152 ms |
| Cache enabled, ETag off | 47 ms | 49 ms |
| Cache enabled, ETag on | 48 ms | 54 ms |
| Cache enabled, conditional requests (304) | 45 ms | 49 ms |
| Cache enabled, every request a distinct key | 148 ms | 157 ms |

## Latency p99

| Scenario | `memory` | `redis` |
|---|---:|---:|
| Cache disabled (reference) | 196 ms | 196 ms |
| Cache enabled, ETag off | 57 ms | 62 ms |
| Cache enabled, ETag on | 58 ms | 65 ms |
| Cache enabled, conditional requests (304) | 58 ms | 67 ms |
| Cache enabled, every request a distinct key | 197 ms | 216 ms |

No failed requests were recorded in any scenario.

## What each scenario measures

- **Cache disabled (reference)** - the plugin is off. Every other row is measured against this.
- **Cache enabled, ETag off** - the plain cache hit path.
- **Cache enabled, ETag on** - as above, plus computing and sending an ETag.
- **Cache enabled, conditional requests (304)** - what a browser or CDN does on revalidation: a matching `If-None-Match`, answered with an empty 304.
- **Cache enabled, every request a distinct key** - the honest downside. Query parameters are part of the cache key, so an API called with a different filter every time stores an entry per request and reads none of them back.

