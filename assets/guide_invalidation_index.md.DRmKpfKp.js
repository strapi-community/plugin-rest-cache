import{_ as c,C as p,o as a,c as u,j as o,a as i,t as g,a1 as s,b as r,w as t,E as l,a2 as d}from"./chunks/framework.90UQlTmt.js";const T=JSON.parse('{"title":"Invalidation","description":"","frontmatter":{"title":"Invalidation"},"headers":[],"relativePath":"guide/invalidation/index.md","filePath":"guide/invalidation/index.md","lastUpdated":1786660168000}'),k={name:"guide/invalidation/index.md"},m={id:"frontmatter-title",tabindex:"-1"};function y(h,e,E,f,b,w){const n=p("Mermaid");return a(),u("div",null,[o("h1",m,[i(g(h.$frontmatter.title)+" ",1),e[0]||(e[0]=o("a",{class:"header-anchor",href:"#frontmatter-title","aria-label":'Permalink to "{{ $frontmatter.title }}"'},"​",-1))]),e[3]||(e[3]=s('<p>A cached response is a copy of something that can change. Invalidation is how the plugin throws that copy away when it does — automatically, for every write, without you writing any code.</p><div class="tip custom-block"><p class="custom-block-title">Since 5.1.0</p><p>Invalidation hooks Strapi&#39;s <strong>document service</strong> rather than HTTP routes. This is <code>strategy.enableDocumentServiceMiddleware</code>, on by default. (<a href="https://github.com/strapi-community/plugin-rest-cache/issues/129" target="_blank" rel="noreferrer">#129</a>)</p></div><h2 id="what-triggers-a-purge" tabindex="-1">What triggers a purge <a class="header-anchor" href="#what-triggers-a-purge" aria-label="Permalink to &quot;What triggers a purge&quot;">​</a></h2><p>Every write in Strapi 5 funnels through the document service, whichever door it came in by. Hooking there means all of these invalidate the cache:</p><ul><li>REST API writes;</li><li>GraphQL mutations;</li><li>the admin panel&#39;s content manager, including bulk actions, clone, publish, unpublish and discard;</li><li>the deprecated entity service;</li><li>scheduled <strong>Content Releases</strong> — content that publishes itself at 3am with no request involved;</li><li>review workflow transitions;</li><li>any <code>strapi.documents(...)</code> call of your own: a service, a cron job, a webhook handler, a migration.</li></ul><p>The route-based approach this replaced could only see writes that arrived on a route it had been told about. It had to carry a hardcoded list of Strapi&#39;s content-manager routes, which drifts every time Strapi adds one, and it was structurally blind to everything in the second half of that list.</p>',6)),(a(),r(d,null,{default:t(()=>[l(n,{id:"mermaid-57",class:"mermaid",graph:"flowchart%20TD%0A%20%20%20%20W(%5BA%20write%2C%20through%20any%20door%20above%5D)%20--%3E%20DS%5B%5BDocument%20service%5D%5D%0A%20%20%20%20DS%20--%3E%20G%7B%22A%20write%20action%2C%3Cbr%2F%3Eon%20a%20cached%20content%20type%3F%22%7D%0A%20%20%20%20G%20--%3E%7Cno%7C%20Skip(%5BNothing%20to%20do%5D)%0A%20%20%20%20G%20--%3E%7Cyes%7C%20S%7BDocument%20id%20known%3F%7D%0A%20%20%20%20S%20--%3E%7Cyes%7C%20T%5BPurge%20that%20document's%20entries%5D%0A%20%20%20%20S%20--%3E%7Cno%7C%20A%5BPurge%20every%20entry%20for%20the%20content%20type%5D%0A%20%20%20%20T%20--%3E%20R%5BAlso%20purge%20related%20types%5D%0A%20%20%20%20A%20--%3E%20R%0A%20%20%20%20R%20--%3E%20Done(%5BWrite%20returns%5D)%0A"})]),fallback:t(()=>[...e[1]||(e[1]=[i(" Loading... ",-1)])]),_:1})),e[4]||(e[4]=s('<p>The first box is the point. A route-based approach can only sit in front of the REST API; every other door in that list changes content without the cache noticing.</p><h3 id="the-actions-that-count-as-writes" tabindex="-1">The actions that count as writes <a class="header-anchor" href="#the-actions-that-count-as-writes" aria-label="Permalink to &quot;The actions that count as writes&quot;">​</a></h3><p><code>create</code>, <code>clone</code>, <code>update</code>, <code>delete</code>, <code>publish</code>, <code>unpublish</code>, <code>discardDraft</code>.</p><p>The middleware runs for reads too — <code>findMany</code>, <code>findOne</code>, <code>count</code> — so filtering to writes is not an optimisation. Without it, populating a cache entry would immediately invalidate it.</p><h3 id="scope-of-each-purge" tabindex="-1">Scope of each purge <a class="header-anchor" href="#scope-of-each-purge" aria-label="Permalink to &quot;Scope of each purge&quot;">​</a></h3><p>The purge is scoped to the document that was written when the plugin can identify it:</p><table tabindex="0"><thead><tr><th>Action</th><th>Document id</th><th>Purge</th></tr></thead><tbody><tr><td><code>create</code>, <code>clone</code></td><td>Minted by the write, read from the <strong>result</strong></td><td>Targeted at the new document</td></tr><tr><td><code>update</code>, <code>delete</code>, <code>publish</code>, <code>unpublish</code>, <code>discardDraft</code></td><td>Carried on the <strong>params</strong></td><td>Targeted at that document</td></tr><tr><td>Anything with no resolvable document id</td><td>—</td><td>Wildcard: every entry for the content type</td></tr></tbody></table><p>Splitting those two cases is deliberate: <code>create</code> and <code>clone</code> mint an id that does not exist until the write returns, so reading it from the params — as every other action requires — would silently produce an unscoped purge on every create.</p><p>A targeted purge matches the content type&#39;s collection route and any route whose parameters it can fill in. See <a href="./../caching/custom-routes.html#route-parameters-and-purging">route parameters and purging</a> for the case where it cannot.</p><p>Content types that are not in your configuration are ignored entirely — the middleware checks before doing any work, so writes to uncached content types cost nothing.</p><h2 id="related-content-types" tabindex="-1">Related content types <a class="header-anchor" href="#related-content-types" aria-label="Permalink to &quot;Related content types&quot;">​</a></h2><p><code>clearRelatedCache</code> (default <code>true</code>) widens each purge to the content types related to the one that changed.</p><p>The relation set is derived from your schemas at boot: everything reachable through <strong>relations</strong> and through <strong>components</strong>, following components into their own attributes until nothing new is found. If an article embeds a component that relates to a category, purging the category also purges the article&#39;s cached responses — because that response contains category data and is now wrong.</p><p>Related content types are purged wildcard-style, since there is no way to know which of their entries embedded the changed document.</p><p>Given a fairly ordinary blog schema:</p>',15)),(a(),r(d,null,{default:t(()=>[l(n,{id:"mermaid-150",class:"mermaid",graph:"erDiagram%0A%20%20%20%20ARTICLE%20%7Do--%7C%7C%20CATEGORY%20%3A%20%22relation%22%0A%20%20%20%20ARTICLE%20%7Do--%7C%7C%20AUTHOR%20%3A%20%22relation%22%0A%20%20%20%20HOMEPAGE%20%7C%7C--o%7B%20SEO_COMPONENT%20%3A%20%22component%22%0A%20%20%20%20SEO_COMPONENT%20%7Do--%7C%7C%20CATEGORY%20%3A%20%22relation%22%0A"})]),fallback:t(()=>[...e[2]||(e[2]=[i(" Loading... ",-1)])]),_:1})),e[5]||(e[5]=s(`<p>Writing to <code>CATEGORY</code> purges <code>CATEGORY</code>, and also <code>ARTICLE</code> — whose responses embed it — and <code>HOMEPAGE</code>, which reaches it through a component two steps away. The traversal runs to a fixed point, so depth is not a limit.</p><div class="info custom-block"><p class="custom-block-title">This is why purges look wider than the write</p><p>A content type is a member of its own related set, so with <code>clearRelatedCache</code> on, a targeted write also issues a wildcard purge for the content type itself. That is what clears parameterised custom routes like <code>/api/categories/slug/:slug</code> on an ordinary update.</p><p>Turning <code>clearRelatedCache</code> off makes purges precise and makes those routes survive until <code>maxAge</code>. Precision here buys a lower miss rate at the cost of occasional stale responses; the default chooses correctness.</p></div><h2 id="timing-and-failure" tabindex="-1">Timing and failure <a class="header-anchor" href="#timing-and-failure" aria-label="Permalink to &quot;Timing and failure&quot;">​</a></h2><p>The purge is <strong>awaited before the write returns</strong>, so a client that writes and immediately reads sees fresh content.</p><p>It is deliberately not deferred to the database transaction&#39;s commit callbacks. Strapi runs those with <code>forEach</code> and never awaits them, so an async purge registered that way is fire-and-forget and races the write that triggered it — reliably lost on the Redis provider, where a purge is a <code>SCAN</code> plus a round trip.</p><p>The trade-off is visible in one place: content manager bulk operations wrap their loop in a transaction, so the purge lands before that outer commit. A read arriving in that window can repopulate the cache from pre-commit state. That race pre-dates this design and applies equally to the route middleware it replaced. (<a href="https://github.com/strapi-community/plugin-rest-cache/issues/132" target="_blank" rel="noreferrer">#132</a>)</p><p><strong>A failed purge never fails the write.</strong> If the provider is down or errors, the error is logged and the write completes. The cost is a stale entry until <code>maxAge</code>; the alternative — refusing writes because a cache is unwell — is worse.</p><h2 id="what-invalidation-cannot-see" tabindex="-1">What invalidation cannot see <a class="header-anchor" href="#what-invalidation-cannot-see" aria-label="Permalink to &quot;What invalidation cannot see&quot;">​</a></h2><ul><li><strong>Writes that bypass the document service</strong>, such as <code>strapi.db.query(...)</code> or raw SQL. Nothing observes those.</li><li><strong>Changes made outside this Strapi process</strong>: another service writing to the same database, a restore from backup, a manual edit.</li><li><strong>Other instances, when using the memory provider.</strong> Each instance caches and invalidates only its own copy, so a write handled by instance A leaves instance B serving stale responses until they expire. Use the <a href="./../providers/">redis provider</a> if you run more than one instance.</li></ul><p>For those cases, purge explicitly — see <a href="./purging.html">Purging</a>.</p><p><code>maxAge</code> is the backstop under all of them. However invalidation is configured, nothing outlives it.</p><div class="info custom-block"><p class="custom-block-title">Deleting a role empties the cache</p><p>Deleting a users-permissions role resets the entire cache. Permissions decide what a response contains, and there is no way to work out which entries a removed role affected.</p></div><h2 id="the-legacy-route-based-mode" tabindex="-1">The legacy route-based mode <a class="header-anchor" href="#the-legacy-route-based-mode" aria-label="Permalink to &quot;The legacy route-based mode&quot;">​</a></h2><p>Setting <code>enableDocumentServiceMiddleware: false</code> falls back to the previous behaviour: purge middlewares injected onto the non-<code>GET</code> routes of your cached content types, plus — when <code>enableAdminCTBMiddleware</code> is on — onto a hardcoded list of content-manager routes.</p><p>The two modes are mutually exclusive. Running both would purge twice for every routed write, so while the document service middleware is on, the route injection is skipped and <code>enableAdminCTBMiddleware</code> has no effect.</p><p>The fallback exists so that an upgrade has an escape hatch, not because it is equivalent. It cannot see GraphQL mutations, Content Releases, or any write that does not traverse a route it knows about.</p><div class="vp-code-group vp-adaptive-theme"><div class="tabs"><input type="radio" name="group-acgEa" id="tab-7tJZ8Tl" checked><label data-title="JavaScript" for="tab-7tJZ8Tl">JavaScript</label><input type="radio" name="group-acgEa" id="tab-rXjzDY3"><label data-title="TypeScript" for="tab-rXjzDY3">TypeScript</label></div><div class="blocks"><div class="language-js vp-adaptive-theme active"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// file: ./config/plugins.js</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exports</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;rest-cache&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    config: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      strategy: {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // Only if you have a specific reason. The default is \`true\`.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        enableDocumentServiceMiddleware: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        enableAdminCTBMiddleware: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        contentTypes: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;api::article.article&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// file: ./config/plugins.ts</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;rest-cache&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    config: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      strategy: {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // Only if you have a specific reason. The default is \`true\`.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        enableDocumentServiceMiddleware: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        enableAdminCTBMiddleware: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        contentTypes: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;api::article.article&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div></div></div><h2 id="watching-it-happen" tabindex="-1">Watching it happen <a class="header-anchor" href="#watching-it-happen" aria-label="Permalink to &quot;Watching it happen&quot;">​</a></h2><p>With <code>debug: true</code>, every purge is logged with the action, the content type and the scope:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[PURGE] update api::article.article documentId=lxr8ai1cs0a1234</span></span>
<span class="line"><span>[PURGE] delete api::article.article (wildcard)</span></span>
<span class="line"><span>[PURGING]: 14 key(s)</span></span></code></pre></div><p>If a write does not produce one of these lines, either the content type is not in your configuration, or the write did not go through the document service.</p><h2 id="next" tabindex="-1">Next <a class="header-anchor" href="#next" aria-label="Permalink to &quot;Next&quot;">​</a></h2><ul><li><a href="./purging.html">Purging manually</a> — admin UI, HTTP endpoints, services.</li><li><a href="./../caching/">How caching works</a> — the other half of the loop.</li><li><a href="./../admin/">Admin panel</a> — where purge controls appear.</li></ul>`,23))])}const A=c(k,[["render",y]]);export{T as __pageData,A as default};
